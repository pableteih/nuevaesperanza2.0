<%= form_with(model: product) do |form| %>
<% if product.errors.any? %>
<div style="color: red">
  <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

  <ul>
    <% product.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :files, 'Archivos Adjuntos' %>
  <%= form.file_field :files, multiple: true %>
</div>

<div>
  <%= form.label :Cantidad, style: "display: block" %>
  <%= form.number_field :quantity %>
</div>

<div>
  <%= form.label :type_id, style: "display: block" %>
  <%= form.collection_select :type_id, @type, :id, :name, prompt: "Tipo" %>
</div>

<div>
  <%= form.label :composition_id, style: "display: block" %>
  <%= form.collection_select :composition_id, @composition, :id, :name, prompt: "Composición" %>
</div>

<div>
  <%= form.label :locality_id, style: "display: block" %>
  <%= form.collection_select :locality_id, @locality, :id, :name, promp: "Localidad" %>

</div>

<div>
  <%= form.label :client_id, style: "display: block" %>
  <%= form.collection_select :client_id, @clients, :second, :first, prompt: "Cliente" %> </div>

<div>
  <%= form.submit %>
</div>
<% end %>